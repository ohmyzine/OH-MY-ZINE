# OH MY ZINE 記事テンプレートの使い方

`article-template.html` は、VHS記事の見た目・スマホ対応・読了メーターをそのまま使える雛形です。

## 1. 新しい記事ファイルを作る

1. `article-template.html` を複製します。
2. 複製したファイルを、半角英数字とハイフンだけの名前に変更します。
   - 例：`article-denim.html`
   - 日本語、空白、記号はファイル名に使わないのがおすすめです。
3. 元の `article-template.html` は、次の記事でも使うので残します。

## 2. 上から順番に入れ替える

新しいHTMLをテキストエディターで開き、`【ここに` または `【` を検索します。次の順番で置き換えれば完成します。

1. ブラウザ用タイトル
2. 記事説明
3. SNS用タイトル・説明・サムネイル
4. 公開日
5. 記事番号と分類
6. 記事ロゴ
7. 画面に出るタイトル
8. タイトル下の短い説明
9. 文字数と読了時間
10. メインのサムネイル
11. タグ
12. 導入文
13. 目次
14. 本文・写真・写真説明
15. 参考資料

HTML内には `[編集 1]` から `[編集 15]` までコメントを入れています。コメントはブラウザ画面には表示されません。

## 3. 画像を入れる

1. 使用する画像を `images` フォルダに入れます。
2. ファイル名は `denim-main.webp`、`denim-01.webp` のように記事名＋番号にすると管理しやすいです。
3. HTMLの画像部分を変更します。

```html
<img src="images/denim-main.webp" alt="古着店に並ぶデニムジャケット">
```

- `src`：実際の画像ファイル名
- `alt`：画像が見えない人にも内容が伝わる短い説明
- メインサムネイルには `fetchpriority="high"` を残します。
- 本文写真には `loading="lazy" decoding="async"` を残します。
- 表示が重い場合は、写真をWebPへ変換し、長辺1600px程度を目安に縮小します。

## 4. 本文と見出しを増やす

本文は `<p>` と `</p>` の間へ書きます。

```html
<p>ここに本文を書きます。</p>
```

大見出しは、次のまとまりをコピーして使います。

```html
<h2 id="denim-history">デニムの歴史</h2>
<p>ここに本文を書きます。</p>
```

目次のリンク先と見出しのIDは、必ず同じ名前にします。

```html
<li><a href="#denim-history">デニムの歴史</a></li>
<h2 id="denim-history">デニムの歴史</h2>
```

IDには半角英数字とハイフンを使い、同じIDを2回使わないでください。

## 5. 参考資料を書く

記事の最後に、確認した本・雑誌・Webページ・画像の出典を書きます。

- 本・雑誌：著者名『タイトル』出版社、発行年、参照ページ
- Web：ページ名、URL、閲覧日
- 画像：撮影者、提供元、掲載許可の有無

Webページは次のようにリンクにできます。

```html
<li><a href="https://example.com/">参考にしたページ名</a>（閲覧日：2026年9月3日）</li>
```

## 6. FASHIONの記事一覧へ載せる

### 検索用データ

`data/articles.json` の最後の記事の `}` のあとにカンマを付け、次のまとまりを追加します。

```json
{
  "title": "新しい記事タイトル",
  "url": "article-denim.html",
  "category": "FASHION / CULTURE / DENIM",
  "image": "images/denim-main.webp",
  "description": "記事の内容を短く説明します。"
}
```

JSONでは、最後の記事のあとに余分なカンマを付けないでください。

### FASHION画面の記事カード

現在のFASHION画面は `fashion.html` 内に記事カードを直接書いています。VHSの記事カードにある `<article class="fashion-stream-card ...">` から対応する `</article>` までを複製し、リンク・画像・タイトル・タグを新しい記事へ変更します。

## 7. 公開前チェック

- 黄色い「このテンプレートの使い方」枠を、`<details class="article-template-guide" open>` から対応する `</details>` まで削除した
- ページ内に `【` や `】` が残っていない
- タイトル、説明、公開日、文字数、読了時間を更新した
- サムネイル、本文写真、`alt` を更新した
- 目次のリンクを押すと正しい見出しへ移動する
- 参考資料を書いた
- PCとスマホの両方で確認した
- `data/articles.json` と `fashion.html` に新しい記事を追加した

## 消さずに残すもの

次の部分は、サイト全体の見た目や機能に必要なので、基本的には変更しません。

- `<link rel="stylesheet" ...>`
- `<script src="...">`
- 上部のウィンドウとナビゲーション
- `article-reading-progress`（読了メーター）
- ニュースレター欄
- TOPへ戻るボタン
- `soft-cursor`
